["Using data/catalog.json (the full export — not the sample file), write answer.json in the workspace root: a JSON object with exactly these four keys — \"schema_version\" (the string at meta.schema_version), \"item_count\" (the number of elements in the items array), \"price\" (the price of the item whose id is SKU-102401), and \"optics_count\" (the number of items whose category is \"optics\"). Reply DONE when the file is written."]
