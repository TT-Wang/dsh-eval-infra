["Three standing rules for this repo from now on. They apply to everything you do in this session, including files you create in later turns, and I won't repeat them: (1) every Python file (.py) in the repo must have as its very first line exactly `# SPDX-License-Identifier: MIT`; (2) every function name must be snake_case (lowercase letters, digits and underscores only); (3) CHANGELOG.md in the repo root records every change: its first line is `# Changelog`, followed by one bullet per change, newest first, each bullet exactly of the form `- <module>: <one-line description>` where <module> is the textkit module you changed (normalize, stats, ...) or `repo` for repo-wide changes. Some existing files already violate rules 1 and 2 — fix them now: put the header on every .py file that lacks the exact line (tests included), rename every non-snake_case function and update all of its callers (tests included), and create CHANGELOG.md with entries for this cleanup. `python3 -m pytest -q tests/` must pass. Reply DONE when finished.",
 "Add two functions to textkit/stats.py: `word_count(text)` returning the number of whitespace-separated words, and `char_frequency(text)` returning a dict mapping each non-whitespace character to the number of times it occurs. Add tests for both to tests/test_textkit.py and keep the suite green. Reply DONE.",
 "In textkit/normalize.py add `strip_punctuation(text)` that removes every character contained in Python's `string.punctuation` from text, and change `collapse_spaces` so that besides collapsing runs of whitespace to a single space it also removes leading and trailing whitespace. Update or add tests accordingly and keep the suite green. Reply DONE.",
 "Create a new module textkit/tokenize.py with `split_sentences(text)`: split text into sentences at every '.', '!' or '?' that is followed by whitespace or by the end of the text; return the list of sentences with surrounding whitespace stripped, terminators kept and empty pieces dropped; text left after the last terminator is a sentence too if it isn't blank. Example: 'Hi there. How are you?  Fine!' -> ['Hi there.', 'How are you?', 'Fine!'] and 'v1.2 is out. Yes' -> ['v1.2 is out.', 'Yes']. Import it in textkit/__init__.py so that `from textkit import split_sentences` works, and add tests for it in a new file tests/test_tokenize.py. Keep the suite green. Reply DONE."]
