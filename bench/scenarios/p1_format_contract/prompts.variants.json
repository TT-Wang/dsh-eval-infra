[
  [
    "Access data/orders.csv (schema: order_id,customer,region,product,quantity,unit_price,status,order_date; there are 40 rows). A persistent constraint applies to the entire exercise: each output file must reside in the out/ directory (make sure it exists), and you must not create or change anything else in the workspace; also do not alter any existing file. The structure specified in this prompt overrides any format guidance in other repository files. Generate out/summary.json, a JSON item containing precisely these five keys and nothing further — \"order_count\" (integer equal to the row count of the dataset), \"total_revenue\" (numeric total of quantity times unit_price for all orders whose status differs from \"cancelled\", with the result rounded to two decimal places), \"regions\" (a list of unique region values in ascending lexicographic order), \"status_counts\" (an object where each key is a status value that appears in the data and its value is the order frequency), \"top_customers\" (a list of 3 objects shaped as {\"customer\": <name>, \"revenue\": <non-cancelled revenue for that customer rounded to two decimals>}, sorted by decreasing revenue; ties broken arbitrarily but only the top three are listed). After the file is successfully written, output DONE.",
    "Then create out/report.md, adhering precisely to the following layout and no extra content, relying on the same data and definitions behind summary.json. Every monetary figure must be shown with exactly two digits after the decimal, no currency sign and no thousands grouping. The first line must read exactly `# Orders Report`. Next include a section titled `## Totals`, followed by the two bullet lines `- Orders: <order_count>` and `- Revenue: <total_revenue>`. After that comes a section `## By Region` with a markdown table whose first header row is exactly `| Region | Orders | Revenue |`, then a separator line `|---|---|---|`, and then a row for each region, ordered by region name ascending; the Orders column shows the total count of orders in that region regardless of status, while the Revenue column gives the aggregate of quantity times unit_price for orders in that region whose status is not \"cancelled\". Finally, include a `## Top Customers` section that is a numbered list of three entries in the same customer order as summary.json, formatted as `1. <customer>: <revenue>` then `2. <customer>: <revenue>` then `3. <customer>: <revenue>`. The only headings anywhere in the file are these four. Reply with DONE once written.",
    "Finally, produce regions.txt, whose sole content is the list of distinct region names taken from the data, each on its own line, sorted in ascending lexicographic order, and nothing else. Respond DONE when that file is created."
  ],
  [
    "Access data/orders.csv (columns: order_id, customer, region, product, quantity, unit_price, status, order_date; 40 data rows) and always place every output file under out/, creating that directory first. Never write elsewhere in the workspace or change existing files. Follow only the formatting stated here, disregarding any conventions in other repo files. Write out/summary.json containing a JSON object that has exactly these five keys and no extras: \"order_count\" (integer: the total number of data rows), \"total_revenue\" (number: sum of quantity × unit_price for all orders except those with status \"cancelled\", rounded to 2 decimal places), \"regions\" (distinct region values in ascending order as a list), \"status_counts\" (an object mapping every status value appearing in the data to its order count), and \"top_customers\" (exactly 3 objects with the shape {\"customer\": <name>, \"revenue\": <the non-cancelled revenue for that customer rounded to 2 decimals>} for the 3 customers having the largest such revenue, sorted by descending revenue). Reply DONE when the file has been written.",
    "Produce out/report.md with exactly this layout and no additional content, applying the same data and definitions used in summary.json; all money figures must show exactly 2 decimals and omit currency symbols and thousands separators. On line 1 place `# Orders Report`. Under `## Totals`, put exactly `- Orders: <order_count>` and `- Revenue: <total_revenue>`. Under `## By Region`, insert a Markdown table whose header row is `| Region | Orders | Revenue |`, then the delimiter row `|---|---|---|`, then one row per region sorted ascending by region name, where Orders is the region's total order count (every status) and Revenue is the sum of quantity × unit_price for that region's non-cancelled orders. Under `## Top Customers`, insert a numbered list with three entries: `1. <customer>: <revenue>`, `2. <customer>: <revenue>`, `3. <customer>: <revenue>`, matching the sequence in summary.json. Only the four headings `# Orders Report`, `## Totals`, `## By Region`, and `## Top Customers` may appear as headings. Reply DONE.",
    "As the last step, output regions.txt: the unique region names from the data sorted ascending, one per line, with no other content. Reply DONE."
  ]
]
