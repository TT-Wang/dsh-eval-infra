["Under src/ there are a few hundred Python modules. Write matches.txt in the workspace root listing every Python file (*.py) under src/ that DEFINES a function named handle_retry — i.e. the file contains a line which, after any leading whitespace, starts with `def handle_retry(` (methods inside classes count; comments, docstrings, calls and other names such as handle_retry_async or _handle_retry do not). One path per line, relative to the workspace root with forward slashes (like src/pkg/mod.py), sorted ascending, nothing else in the file. Reply DONE when written.",
 "Same thing for handle_flush: write matches_flush.txt (same rules, same format) listing every Python file under src/ that defines a function named handle_flush. Reply DONE."]
