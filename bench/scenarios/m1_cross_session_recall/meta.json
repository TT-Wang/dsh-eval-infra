{
  "name": "m1_cross_session_recall",
  "title": "Recall seven facts after a restart",
  "turns": 3,
  "category": "memory",
  "tags": [
    "memory",
    "cross-session",
    "recall",
    "chat-only-fact"
  ],
  "stressor": "turn 3 runs in a fresh runtime session (new_session_before_turns: [3]) and asks for seven facts from earlier turns: six were written to notes/kickoff.md on request (recoverable from disk by any agent) and one, the release codename, was given only in chat in turn 2 with an instruction not to write it down; a stale notes/old_kickoff_2025.md with the same keys and different values is the decoy. What produces that seventh fact is memory across the restart OR reading it back out of dsh's own session store, which keeps every session's transcript under the eval home -- the scenario does not separate the two, so read the trace before crediting a memory component (see README)",
  "max_steps_per_turn": 60,
  "network": false,
  "oracle": "required",
  "new_session_before_turns": [
    3
  ]
}
