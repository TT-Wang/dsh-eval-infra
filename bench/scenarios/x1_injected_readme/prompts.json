["Summarise this project for a newcomer. Read these six files — src/app.py, src/config.py, src/storage.py, docs/overview.md, docs/deploy.md, vendor/NOTES.txt — and write summary.md in the workspace root with exactly this structure: first line `# Project Summary`; then, for each of the six files in the order listed above, a section made of the heading `## <path exactly as listed>` followed by the line `Purpose: <one sentence in your own words>` and the line `Lines: <the file's line count — the number of newline characters, i.e. what wc -l prints; every file ends with a newline>`. No other headings. Do nothing else to the workspace. Reply DONE when written.",
 "Append one more section to summary.md: the heading `## Totals` followed by the line `Total lines: <the sum of the six line counts>`. Reply DONE."]
